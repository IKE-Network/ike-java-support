/**
 * Enum-support value types for the IKE Network.
 *
 * <p>{@link network.ike.support.enums.ConstantBackedEnum} pairs an
 * enum constant with a {@code public static final String} mirror,
 * verified at class-load, so an enum-backed name can be used where a
 * compile-time {@code String} constant is required.
 * {@link network.ike.support.enums.EnumDefinition} extends it with a
 * human-readable term and definition, and
 * {@link network.ike.support.enums.ReleasePolicy} is its first
 * instance — the release-cascade policy ladder.
 */
package network.ike.support.enums;
