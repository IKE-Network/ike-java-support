/**
 * Shared, zero-dependency value types for the IKE Network.
 *
 * <p>{@link network.ike.support.ConstantBackedEnum} pairs an enum
 * constant with a stable string literal usable where a compile-time
 * {@code String} constant is required. {@link
 * network.ike.support.EnumDefinition} extends it with a human-readable
 * term and definition, and {@link network.ike.support.ReleasePolicy}
 * is its first instance — the release-cascade policy ladder.
 */
package network.ike.support;
