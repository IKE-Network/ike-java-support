/**
 * IKE Java Support — shared, zero-dependency value types.
 *
 * <p>This module depends on nothing but {@code java.base}; the
 * {@code maven-enforcer} build keeps it that way.
 */
module network.ike.support {
    exports network.ike.support.enums;
}
